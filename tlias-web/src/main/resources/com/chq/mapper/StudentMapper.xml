<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chq.mapper.StudentMapper">
    <insert id="addStudent">
        Insert into student(name, no, gender, phone, degree, clazz_id, id_card, is_college, address,graduation_date,update_time)
                values (#{name}, #{no}, #{gender}, #{phone}, #{degree}, #{clazzId}, #{idCard}, #{isCollege},#{address}, #{graduationDate},#{updateTime})
    </insert>
    <delete id="deleteStudent">
        DELETE FROM student WHERE id IN
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <select id="findStudents" resultType="com.chq.pojo.Student">
        SELECT stu.*,
        clz.name AS clazz_name
        FROM student AS stu
        LEFT JOIN clazz AS clz ON clz.id = stu.clazz_id
        <where>
            <if test="name != null">
                AND stu.name LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="degree != null">
                AND stu.degree = #{degree}
            </if>
            <if test="clazzId != null">
                AND stu.clazz_id = #{clazzId}
            </if>
        </where>
        ORDER BY stu.update_time DESC
    </select>
</mapper>